{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build",
    "watchPatterns": ["server/**", "dist/public", "vite.config.js"]
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
