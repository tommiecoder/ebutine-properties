{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build",
    "watchPatterns": ["server/**", "dist/**", "vite.config.js"]
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
